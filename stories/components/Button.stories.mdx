import {Canvas, Meta, Story, ArgsTable} from '@storybook/addon-docs';


<Meta
    title="Components/Buttons"
    argTypes={{
        size: {
            name: 'size',
            options: ['large', 'small'],
            control: {type: 'radio'},
            description: 'Button size',
            defaultValue: 'large',
            table: {
                type: {summary: 'string'},
                defaultValue: {summary: 'large'}
            }
        }
    }}
/>

export const Template = ({size}) => {
    return `<button class="play-button--${size} custom-font" >Let's play!</button>`
}

# Buttons

The sole purpose of the buttons on the website is to open the game screen to start playing.
The game screen opens in the same window.

<Canvas>
    <Story name="Buttons">
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Buttons"/>