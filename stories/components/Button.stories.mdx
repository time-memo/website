import {Canvas, Meta, Story, ArgsTable} from '@storybook/addon-docs';


<Meta
    title="Buttons"
    argTypes={{
        size: {
            name: 'size',
            options: ['large', 'small'],
            control: {type: 'radio'},
            description: 'Button size',
            defaultValue: 'large',
            table: {
                type: {summary: 'string'},
                defaultValue: {summary: 'large'}
            }
        }
    }}
/>

export const Template = ({size}) => {
    const btn = document.createElement('button');
    btn.className = `play-button--${size}`
    btn.innerText = `Let's play`;
    return btn
}

# Buttons

<Canvas>
    <Story name="Buttons">
        {Template.bind({})}
    </Story>
</Canvas>

<ArgsTable story="Buttons"/>